import { Workout } from "../models/WorkoutModel";
import SleepModel from "../models/SleepModel";
class TrackerServices {
  static async getScheduleService({
    date,
    userId,
  }: {
    date: string | Date;
    userId: string;
  }) {}
}

export default TrackerServices;
